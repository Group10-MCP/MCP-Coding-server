# services/gemini.py

class GeminiService:
    def ask(self, prompt: str) -> str:
        # TODO: Replace this with real Gemini API integration.
        # For now, just simulate a response.
        return f"[GeminiService response] {prompt}"
